#include <stdio.h>

/**
 * main - prints the phrase
 * Return: 0 if existed properly, non-zero if not.
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
